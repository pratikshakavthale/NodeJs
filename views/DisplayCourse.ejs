<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Management System</title>
</head>
<body>
    <form action="/add">
        <button type="submit" name="buttonadd" id="add" value="add">Add New Course</button>
    </form>

    <table border="1">
        <tr>
            <th>Course Id</th>
            <th>Course Name</th>
            <th>Course Fees</th>
            <th>Duration</th>
        </tr>
        <% for(var i=0;i<coursedata.length;i++){%>
            <tr>
                <td><%=coursedata[i].id%></td>
                <td><%=coursedata[i].cname%></td>
                <td><%=coursedata[i].fees%></td>
                <td><%=coursedata[i].duration%></td>
                <td>
                    <a href="deleteCourse">Delete</a>
                    <a href="updatecourse/<%=coursedata[i].id%>">Update</a>
                </td>
            </tr>
        <%}%>
    </table>
</body>
</html>